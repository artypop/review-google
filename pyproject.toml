[project]
name = "review-google"
version = "0.1.0"
description = "ReviewFlowz — analyse des suppressions d'avis Google"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.5",
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
    "scikit-learn>=1.9.0",
    "statsmodels>=0.15.0",
]
